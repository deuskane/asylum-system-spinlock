**Table Of Contents**
- **Introduction**: Short description of this repository and purpose.
- **HDL Modules**: Per-module description, generics and ports.
- **Register Map (CSR)**: CSR register list with links to registers and bitfields.
- **Verification**: Notes about simulation and the Fusesoc core file.

**Introduction**
- **Repository**: `asylum-system-spinlock` provides a small General Interrupt Controller (spinlock) implementation in VHDL.
- **Location of sources**: HDL sources are in `hdl/`. CSR definitions and generated files are in `hdl/csr/`.
- **Purpose**: Provide a lightweight spinlock and an SBI-compatible interface.

**HDL Modules**
- **Module folder**: `hdl/`

Below are the modules present in `hdl/` with their generics (if any) and port descriptions.

**sbi_spinlock** (`hdl/sbi_spinlock.vhd`)
- **Description**: Top-level integration wrapper that instantiates the CSR register block (`spinlock_registers`). Exposes an SBI-compatible bus interface.

Generics

| Name | Type | Default | Description |
|------|------|---------|-------------|
| *(none)* | | | This entity defines no generics |

Ports

| Name | Direction | Type | Description |
|------|-----------|------|-------------|
| `clk_i` | in | `std_logic` | Clock input |
| `arst_b_i` | in | `std_logic` | Asynchronous reset (active low) |
| `sbi_ini_i` | in | `sbi_ini_t` | Bus initiator interface (SBI) |
| `sbi_tgt_o` | out | `sbi_tgt_t` | Bus target interface (SBI) |

**spinlock_pkg** (`hdl/spinlock_pkg.vhd`)
- **Description**: Package declaring component interfaces and record/array types used by the spinlock family.

**Register Map (CSR)**
- **CSR folder**: `hdl/csr/`
- **Primary files**: `hdl/csr/spinlock.hjson`, `hdl/csr/spinlock_csr.md`, `hdl/csr/spinlock_csr.vhd`, `hdl/csr/spinlock_csr.h`.
- **Generated by**: The project `spinlock.core` uses the `regtool` generator (see `spinlock.core`) to produce CSR HDL and header artifacts from `hdl/csr/spinlock.hjson`.
